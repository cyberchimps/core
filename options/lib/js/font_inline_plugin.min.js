(function (a) {
	a.fn.google_fonts = function (b) {
		var c = {fontname: "arial"};
		var b = jQuery.extend(c, b);
		var e = b.fontname;
		e = e.replace(/ /gi, "+");
		var d = window.location.protocol;
		a("head").append('<link href="' + d + "//fonts.googleapis.com/css?family=" + e + '" rel="stylesheet" type="text/css">');
		jQuery(this).css("font-family", b.fontname);
		return this
	}
})(jQuery);